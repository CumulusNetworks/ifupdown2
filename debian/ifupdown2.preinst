#! /bin/sh
set -e
case "$1" in
    install|upgrade)
        if [ -f /etc/default/networking/networking.default ]; then
            dpkg-maintscript-helper rm_conffile /etc/default/networking/networking.default 1$
            rm -rf /etc/default/networking
        fi
        ;;
esac
